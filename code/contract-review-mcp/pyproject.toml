[project]
name = "contract-review-mcp"
version = "0.1.0"
description = "Contract Review MCP Server for Neo4j integration"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "fastmcp>=0.1.0",
    "python-dotenv>=1.0.0",
]
